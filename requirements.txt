# --- Core pipeline (main.py) ---
pandas>=2.0
numpy>=1.24
nltk>=3.8
scikit-learn>=1.3
sentence-transformers>=2.2
transformers>=4.36
torch>=2.0
rouge-score>=0.1.2
# Hugging Face stack (sometimes not pulled on minimal installs)
huggingface-hub>=0.20
safetensors>=0.4
tokenizers>=0.15

# --- GUI ---
streamlit>=1.28

# --- Jupyter notebook (run_summarizer.ipynb) ---
ipykernel>=6.0
notebook>=7.0.0
